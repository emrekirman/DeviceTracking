/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FramPipe } from './Fram.pipe';

describe('Pipe: Frame', () => {
  it('create an instance', () => {
    let pipe = new FramPipe();
    expect(pipe).toBeTruthy();
  });
});
